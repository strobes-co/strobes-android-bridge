<?xml version="1.0" encoding="utf-8"?>
<!-- Plain white oval — tinted per-state in code (connected/disconnected/etc).
     One drawable + a tint call beats four near-identical color variants. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white" />
</shape>
